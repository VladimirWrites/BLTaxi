package com.vlad1m1r.baseui.theme

import androidx.compose.ui.graphics.Color

// Light theme colors
val PrimaryLight = Color(0xFF222222)
val PrimaryLightVariant = Color(0xFF333333)
val PrimaryDark = Color(0xFF111111)

val SecondaryLight = Color(0xFFFFCF75)
val SecondaryLightVariant = Color(0xFFFFFFA5)

val SurfaceLight = Color(0xFFEEEEEE)
val BackgroundLight = Color(0xFFFFFFFF)
val OnPrimaryLight = Color(0xFFFFFFFF)
val OnSecondaryLight = Color(0xFFFFFFFF)
val OnSurfaceLight = Color(0xFF000000)
val OnBackgroundLight = Color(0xFF000000)

// Dark theme colors
val PrimaryNight = Color(0xFFFFFFFF)
val PrimaryNightVariant = Color(0xFF333333)
val PrimaryDarkNight = Color(0xFF111111)

val SecondaryNight = Color(0xFFFFCF75)
val SecondaryNightVariant = Color(0xFFFFFFA5)

val SurfaceNight = Color(0xFF222222)
val BackgroundNight = Color(0xFF111111)
val OnPrimaryNight = Color(0xFF000000)
val OnSecondaryNight = Color(0xFFFFFFFF)
val OnSurfaceNight = Color(0xFFFFFFFF)
val OnBackgroundNight = Color(0xFFFFFFFF)

// Common colors
val ErrorColor = Color(0xFFD34F28)
val OnErrorColor = Color(0xFFFFFFFF)

// Checker pattern colors
val CheckerDarkSquareLight = Color(0xFF888888)  // Darker gray for light theme
val CheckerDarkSquareNight = Color(0xFF333333)  // Darker gray for dark theme
val CheckerLightSquareLight = SurfaceLight       // Card surface color in light theme
val CheckerLightSquareNight = Color(0xFF999999)  // Matches the muted on-surface tone in dark theme

// Transparent color for buttons
val TransparentColor = Color(0x00000000)
